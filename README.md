# Система управления объявлениями для модерации

Веб-приложение для модерации объявлений. Упрощённая версия внутренней системы, которую используют модераторы для проверки и управления объявлениями пользователей.

## 🚀 Технологии

### Обязательные требования
- **Node.js v20** - среда выполнения
- **React v18+** - библиотека для создания пользовательского интерфейса
- **react-router-dom** - маршрутизация в приложении
- **TypeScript** - типизированный JavaScript для повышения надёжности кода
- **tech-int3-server** - Express.js API сервер (обязательное условие)

### Дополнительные технологии (со звёздочкой)
- **Vite** - быстрый сборщик и dev-сервер
- **Ant Design** - комплексная UI-библиотека с готовыми компонентами
- **React Query (@tanstack/react-query)** - управление серверным состоянием и кэшированием
- **Axios** - HTTP-клиент с поддержкой отмены запросов
- **Recharts** - библиотека для построения графиков и диаграмм
- **Day.js** - работа с датами

### Обоснование выбора технологий

1. **Vite** - выбран вместо Create React App из-за:
   - Молниеносной скорости сборки и HMR
   - Нативной поддержки ES-модулей
   - Оптимизированной production-сборки

2. **Ant Design** - выбран из-за:
   - Богатого набора готовых компонентов
   - Встроенной поддержки тёмной темы
   - Отличной документации и сообщества
   - Адаптивности и accessibility

3. **React Query** - выбран вместо Redux из-за:
   - Упрощённой работы с серверным состоянием
   - Автоматического кэширования и синхронизации
   - Встроенной поддержки загрузки и ошибок
   - Меньшего boilerplate кода

4. **TypeScript** - выбран для:
   - Типобезопасности на этапе разработки
   - Лучшей поддержки IDE
   - Упрощения рефакторинга больших проектов

## 📋 Функциональность

### 1. Главная страница - Список объявлений (`/list`)

- ✅ Отображение объявлений в виде карточек
- ✅ Фильтрация:
  - По статусу (множественный выбор)
  - По категории
  - По диапазону цен
  - Поиск по названию
- ✅ Сортировка:
  - По дате создания (новые/старые)
  - По цене (возрастание/убывание)
  - По приоритету
- ✅ Пагинация (10 объявлений на страницу)
- ✅ URL-синхронизация фильтров
- ✅ Bulk-операции (массовое одобрение/отклонение)
- ✅ Сохранение наборов фильтров
- ✅ Real-time обновления (автообновление каждые 30 сек)
- ✅ Счётчик новых объявлений

### 2. Страница детального просмотра (`/item/:id`)

- ✅ Полная информация об объявлении:
  - Галерея изображений (карусель)
  - Описание и характеристики
  - Информация о продавце
- ✅ История модерации
- ✅ Панель действий:
  - Одобрить
  - Отклонить (с выбором причины)
  - Вернуть на доработку
- ✅ Навигация между объявлениями (Предыдущее/Следующее)

### 3. Страница статистики (`/stats`)

- ✅ Метрики:
  - Всего проверено
  - Процент одобренных
  - Процент отклонённых
  - Среднее время проверки
- ✅ Графики:
  - График активности по дням (столбчатая диаграмма)
  - Распределение решений (круговая диаграмма)
  - Распределение по категориям
- ✅ Фильтр по периоду (Сегодня / 7 дней / 30 дней)
- ✅ Экспорт статистики в CSV и PDF

### Дополнительные возможности

- ✅ **Горячие клавиши:**
  - `A` - одобрить объявление
  - `D` - отклонить объявление
  - `→` - следующее объявление
  - `←` - предыдущее объявление
  - `/` - фокус на поиск

- ✅ **Тёмная тема** с сохранением в localStorage
- ✅ **Отмена запросов** при переходе между страницами
- ✅ **Анимации** (плавные переходы, появление карточек)
- ✅ **Адаптивный дизайн** для мобильных устройств

## 🛠 Установка и запуск

### Предварительные требования

- Node.js v20 или выше
- npm или yarn

### Локальная разработка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd FRONTAV
```

2. **Установите зависимости клиента:**
```bash
npm install
```

3. **Установите зависимости сервера:**
```bash
cd tech-int3-server
npm install
cd ..
```

4. **Запустите API сервер:**
```bash
npm run server
```
Сервер будет доступен на `http://localhost:3001`

5. **В другом терминале запустите клиент:**
```bash
npm run dev
```
Приложение будет доступно на `http://localhost:3000`

### Docker Compose (рекомендуется)

1. **Соберите и запустите контейнеры:**
```bash
docker-compose up --build
```

2. **Приложение будет доступно на:**
   - Клиент: `http://localhost:3000`
   - API: `http://localhost:3001`

3. **Остановить контейнеры:**
```bash
docker-compose down
```

### Production сборка

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 📁 Структура проекта

```
FRONTAV/
├── tech-int3-server/       # Express.js API сервер
│   ├── server.js          # Главный файл сервера
│   ├── schema.yaml        # OpenAPI документация
│   ├── src/
│   │   ├── controllers/   # Контроллеры
│   │   ├── models/        # Модели данных
│   │   ├── routes/        # Маршруты API
│   │   └── middleware/    # Middleware
│   └── package.json
├── src/
│   ├── api/               # API клиент
│   │   └── client.ts      # Axios клиент с отменой запросов
│   ├── components/        # Переиспользуемые компоненты
│   │   └── AppHeader.tsx  # Шапка приложения
│   ├── hooks/             # Кастомные хуки
│   │   ├── useTheme.ts    # Управление темой
│   │   └── useHotkeys.ts  # Обработка горячих клавиш
│   ├── pages/             # Страницы приложения
│   │   ├── AdsList.tsx    # Список объявлений
│   │   ├── AdDetail.tsx   # Детальный просмотр
│   │   └── Statistics.tsx # Статистика
│   ├── types/             # TypeScript типы
│   │   └── index.ts
│   ├── App.tsx            # Главный компонент
│   ├── main.tsx           # Точка входа
│   └── index.css          # Глобальные стили
├── docker-compose.yml     # Docker Compose конфигурация
├── Dockerfile             # Docker образ для клиента
├── nginx.conf             # Nginx конфигурация
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

## 🎯 API Endpoints

API сервер (tech-int3-server) предоставляет следующие endpoints:

**Объявления:**
- `GET /api/v1/ads` - получить список объявлений с фильтрацией и пагинацией
- `GET /api/v1/ads/:id` - получить объявление по ID
- `POST /api/v1/ads/:id/approve` - одобрить объявление
- `POST /api/v1/ads/:id/reject` - отклонить объявление (требует reason в body)
- `POST /api/v1/ads/:id/request-changes` - запросить изменения (требует reason в body)

**Статистика:**
- `GET /api/v1/stats/summary` - получить общую статистику (параметр period: today/week/month)
- `GET /api/v1/stats/chart/activity` - получить данные для графика активности
- `GET /api/v1/stats/chart/decisions` - получить данные для графика решений
- `GET /api/v1/stats/chart/categories` - получить данные для графика категорий

Полная документация API доступна в файле `tech-int3-server/schema.yaml` (OpenAPI 3.0)

## ⌨️ Горячие клавиши

- `A` - Одобрить текущее объявление (на странице детального просмотра)
- `D` - Отклонить текущее объявление (на странице детального просмотра)
- `→` - Перейти к следующему объявлению
- `←` - Перейти к предыдущему объявлению
- `/` - Установить фокус на поле поиска (на странице списка)

## 🎨 Тёмная тема

Переключатель темы находится в правом верхнем углу. Выбор сохраняется в `localStorage` и восстанавливается при следующем посещении.

## 📊 Статистика

На странице `/stats` доступна статистика модератора:
- Общие метрики за выбранный период
- График активности по дням
- Распределение решений (одобрено/отклонено/на доработке)
- Распределение по категориям
- Экспорт данных в CSV и PDF

## 🧪 Тестирование

Для запуска линтера:
```bash
npm run lint
```

Для форматирования кода:
```bash
npm run format
```

## 📝 Скрипты

- `npm run dev` - запуск dev-сервера
- `npm run build` - production сборка
- `npm run preview` - предпросмотр production сборки
- `npm run server` - запуск API сервера (tech-int3-server)
- `npm run lint` - проверка кода линтером
- `npm run format` - форматирование кода

## 🔧 Конфигурация

### Vite
Конфигурация находится в `vite.config.ts`. Настроен proxy для API запросов на `/api/v1`.

### TypeScript
Строгий режим включён в `tsconfig.json` для максимальной типобезопасности.

### ESLint
Настроен для React и TypeScript в `.eslintrc.cjs`.

## 🐛 Известные ограничения

- Изображения используют placeholder-сервис (placehold.co)
- Авторизация не реализована (согласно требованиям)
- Данные хранятся в памяти сервера и сбрасываются при перезапуске

## 📄 Лицензия

См. файл LICENSE

## 👤 Автор

Тестовое задание для стажёра Frontend (осенняя волна 2025)

---

**Примечание:** Это тестовое задание. В production-версии необходимо добавить:
- Реальную авторизацию
- Обработку ошибок сети
- Unit-тесты
- E2E тесты
- Оптимизацию изображений
- Service Worker для офлайн-работы

